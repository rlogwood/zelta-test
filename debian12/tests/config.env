# names of the ZFS pools that we create for testing
export APOOL_NAME="apool"
export BPOOL_NAME="bpool"

# names of the storage devices assicated with each pool
# in this example, 2 nvme drives with a partition at p1 are used"
export APOOL_DISK="/dev/nvme?n1p1"
export BPOOL_DISK="/dev/nvme?n1p1"
